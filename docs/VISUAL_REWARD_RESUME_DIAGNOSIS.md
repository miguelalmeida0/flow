# Visual reward resume diagnosis — 2026-09-05

The checkpoint's motion gate failed on running native animations after the unchanged 900 ms settle period. A fresh instrumented reproduction captured the owner and clocks in `artifacts/reward-resume-diagnostics/unsettled-animation-before-clock-fix.json`:

- target: the real `BreathingRoomBlock`, `aria-label="Breathing room, 20 minutes"`;
- property: opacity 0 → 1, native WAAPI, one 720 ms ease-in-out iteration;
- animation currentTime: 712.3 ms; startTime: 3688 ms;
- native document timeline: 4400.3 ms;
- simulated performance clock: 4701 ms.

The test's installed Playwright clock supplied Motion with a different start-time origin/progression than native WAAPI. A second reproduction using `clock.setFixedTime` retained the defect: this Playwright runtime also installs its simulated performance/rAF implementation for that API. Increasing the wait would conceal the measurement problem.

The performance/stress fixture now shifts only `Date` through an initialization proxy. Time continues to elapse for cooldowns and domain timestamps; native `performance.now`, requestAnimationFrame, timers and WAAPI remain untouched. The same 900 ms settle, all performance thresholds, zero residue assertions, 120 signature runs and interruptions remain enforced. The first corrected full signature run passed all 120 in 4.6 minutes.

The same clock defect affected the old paused-clock frame matrix, so that harness was repaired before the final release. Setup and trigger are separate, the zero frame is genuinely before the trigger, intermediate frames use native deadlines, and the final frame verifies complete native settle. All 144 frames now carry requested/actual capture timing and native animation diagnostics. Command-surface inspection also requires an existing, visibly painted element rather than treating an absent rectangle as contained. The first corrected native 144-frame run passed in 1.1 minutes.

The first full release then caught a second evidence race: Time travel's real Level-1 acknowledgment lasts 180 ms, while trace-backed command completion could return after it had settled. Its trace showed the five lenses moving, but an after-the-fact active-state poll missed the transient state. The frame harness now observes the existing diagnostic snapshot publication before the trigger and requires a fresh event ID, the expected level, and an actual active publication. It records that publication's native timestamp and restores the diagnostic property afterward. It neither extends the cue nor writes domain/presentation state. The failed run remains recorded in `artifacts/reward-resume-diagnostics/native-frame-observation-failure.json`.

A separate real production defect was identified in the same owners: `EventBlock`, `DayTimeline` and `BreathingRoomBlock` read only Motion's OS preference, so choosing Reduced inside Flow did not switch their own recipes. They now consume the shared application preference; integration and real-browser mode checks cover it.

Independent live UI inspection also confirmed that importance stars initialized opacity to zero without ever animating it to one. The final state now explicitly sets opacity to one in both modes, and production browser assertions inspect computed opacity for existing and newly important events. Focus copy was changed to describe protected time and actual elapsed time rather than internal transaction/reward terms.

No scheduling, interpretation, persistence, or history transformation was changed in that initial lifecycle repair. The later independently discovered read-only recommendation alias is described below. Final release counts and independent verdict belong in `docs/quality/VERIFICATION.md`; a targeted pass alone is not the release verdict.

## Measured rendering cost and final visual review repairs

A later full-run native performance failure was preserved in `normal-performance-threshold-failure.json`: normal maximum gap 366.7 ms, estimated miss rate 19.17%, and maximum long task 129 ms. Settle, DOM, listener, clone, and sound invariants remained clean. Read-only host observations also showed CPU/memory pressure; unrelated workloads were left untouched.

Rather than retrying that full suite blindly, an opt-in diagnostic CPU profile (`FLOW_PROFILE_MOTION=1`) sampled the exact 20-cycle workload. Its dominant named JavaScript frame was `formatTime`: repeated construction of `Intl.DateTimeFormat` accounted for 2,897 ms of sample-attributed self time. Reusing two fixed English clock formatters reduced that attribution to 198 ms (about 93%); all 1,441 supported minute labels are regression-tested against the previous implementation. The profile also exposed unnecessary Home projection work in a mascot that returned null on utility routes; hidden characters no longer compute that model. Neither change touches scheduling or history semantics.

Profiler-on evidence is diagnostic, not the default release gate. Its remaining missed-frame/throttled-gap failures are preserved in `profiled-performance-metrics.json` and `optimized-profiled-performance-metrics.json`. The subsequent default, profiler-off focused check passed with a 16.8 ms normal maximum gap, 66.6 ms deliberately throttled maximum, zero estimated misses/long tasks, node delta +15, listener delta −4, and zero residue. The complete final gate is still required.

Independent review of actual native screenshots found three additional presentation defects: a mobile ceremony label overlapped page eyebrows, the fixed Home mascot covered wrapped/short lens layouts, and pre-trigger Commitment frames retained a previous setup flight. Labels now occupy the clear mobile header gap; a measured, event-driven mascot safe zone yields to actual cards/controls and reconnects on temporal replacement; and zero-percent frames require all setup flights/native animation to settle. Frame audits enforce heading/eyebrow and Home-lens collisions. Shared-flight user copy names the meaningful transformation instead of exposing internal choreography beat names; diagnostic beat attributes remain unchanged.

The final contract review also caught overly persistent Home character presence. Semantic state now has an explicit sparse-visibility policy: no hydration/rest, normal navigation/edit, focused-work, or stale post-ceremony mascot. Actual listening/understanding or an active earned Level-3 moment may appear only in measured clear space. The production geometry test starts one injected browser Live Session, checks wrapped/short/mobile layouts plus temporal replacement, Back and cross-tab Level-0 adoption, then verifies disappearance when that session stops. This is synthetic browser evidence, not physical acoustic acceptance.

A production reward regression exposed a second event from one Capture→Outcome request: the 700 ms destination-shell callback called ordinary navigation and replaced the transaction's facts with a world-opened acknowledgment. The shared flight now performs a quiet internal route handoff, preserving its single committed reward. Controller integration and real-browser assertions inspect the full settled flight and require exactly one reward/history addition. Ordinary explicit navigation still acknowledges normally.

## Remaining frame-budget attribution

The next complete run preserved a genuine 8.505% estimated normal missed-frame rate against the unchanged 8% budget (`post-repair-miss-rate-failure.json`). Its other thresholds passed, as did all 120 lifecycle runs, 144 frame captures and the recording. This was not rounded into a pass.

A fresh matching-bundle profile (`remaining-performance-attribution.cpuprofile`) found 183 ms of sample-attributed Motion scroll measurement, 123 ms in React commit, 37 ms in repeated clock formatting, and five remaining Home date formatter constructions. The 422 ms attributed to atomic persistence was left intact. Profiling overhead is recorded separately and is not acceptance evidence.

The bounded optimization caches only valid integer wall-minute labels (0–1440); fractional, out-of-range and invalid inputs retain their prior behavior. Two measured Home date formatter configurations are reused, with leap-day/year-boundary equivalence tests. There is no unbounded date-label cache.

Installed Motion 12.43.0 delegates layout to `MeasureLayoutWithContext.getSnapshotBeforeUpdate`: a supplied unchanged `layoutDependency` skips `projection.willUpdate()`. Its `nodeGroup` propagates real layout dirtiness to neighbors; native resize handling independently forces layout measurement. Event dependencies now include actual date/position/height/mobility/protection/selection/manipulation geometry. The calendar group's header also includes density, preview, deferred summary, the relationship inspector, and the compact Now label, since those can move the timeline. Feedback and reward state are deliberately absent.

The first new browser assertion exposed the header's old unconditional propagation: one feedback-only command measured all eight events twice. The actual header dependency fixed that cause; the unchanged assertion then passed with zero event geometry reads, while move/undo still required real measurements and exact history. Eight existing neighbor-reflow, geometry, pointer drag/resize, mobile/reduced-motion and keyboard tests also passed. The initial failed trace is retained under `unconditional-header-measurement-failure/`. Three consecutive profiler-off 20-cycle checks are required before another full release; none may be discarded or selected as the lucky pass.

## Final review: text geometry and actual intermediate evidence

The first three-run batch was FAIL/FAIL/PASS and was preserved in full. Native idle controls and script attribution showed material shared-host scheduling variation, without altering unrelated workloads. The authorized fresh batch then passed all three default runs; the next complete motion gate passed 4/4 journeys, 120 signature repetitions and all automated frame checks. The outer release still failed the original legacy morph guard at 87 ms against its unchanged 50 ms limit. No functional release pass was inferred from that partial run.

Independent review of all 144 PNGs identified two additional real issues. First, the shared flight's anisotropic surface transform compressed its text. The clone now has an independently scaling paper surface and unscaled co-moving text, still one aria-hidden clone and one transform owner per element. A production mobile regression measures actual rendered title dimensions during the transformation and passed 1/1.

Second, sequential screenshots truthfully recorded late intervals but did not supply intermediate motion coverage. Native CDP PNG capture now records actual compositor frame-swap timestamps, immediately acknowledges frames, and selects distinct real samples within a fixed ±60 ms bound. All raw timestamps and read-only layout/animation observations are preserved. The existing Home temporal scene is 480 ms; its native WAAPI start and shared existing duration token now anchor Time travel rather than the unrelated 180 ms acknowledgment. No product animation was lengthened. Zero frames precede the trigger, final frames require native settle, and missing intermediate coverage fails. The matrix owns the compositor stream while action/DOM/source traces remain enabled, avoiding a competing trace JPEG recorder. Native Enter submits the already-filled real production field; no domain or parser seam is used.

The repaired targeted matrix passed all 144 frames: its 96 actual intermediate frames were within 9.70 ms of their requested native time, with a maximum native audit skew of 8.34 ms. Independent review inspected all 144 and approved both the readable unscaled clone and actual intermediate coverage. Early native-capture selection/recorder failures are preserved under `native-frame-first/` through `native-frame-fifth/`; they were not represented as passing evidence.

The single authorized legacy LoAF diagnostic then passed all 20 cold morph cycles: median 11.6 ms, zero measured transition long tasks, zero estimated dropped frames, and no clones, frame loops or browser errors. Native attribution recorded a 62.4 ms initial module-evaluation task before any cycle, but no long task during a cycle. The earlier 87 ms failure was not reproduced and is not retrospectively explained or waived. Its original 50 ms limit and production measurement logic remain unchanged; the complete default release must still pass independently of this diagnostic.

## Independent release findings and actual development coverage

The next complete automated release passed, but independent QA found a real Home → Today transition defect: the shared layout identity connected a tall Home lens to a shallow page header, so its text inherited a measured `scale(0.301038, 4.05172)` and crossed the Now region. Shared world continuity now belongs to an aria-hidden paper surface. Live headings and content remain outside its anisotropic projection, with position-only layout where needed. A native 1280 × 800 route-entry test captures five distinct compositor intermediates plus before/settled images and audits actual text scale and Now separation throughout entry. Root inspected all seven images and approved the bounded repair. The same paper/content boundary is shared by utility-world headers.

QA also correctly rejected production StrictMode wrapping as evidence of development effect probes. The new actual Vite development matrix reuses the six production signature fixtures in Full and Reduced motion. A read-only standard DevTools hook proves the development renderer, StrictEffectsMode and observed native probes. Every fresh reward is interrupted while active through an enabled real UI control; exact history or non-mutating temporal projection, resize/visibility/route cancellation, one reward per logical request and zero settled residue are asserted. The separate microphone-acquisition test remains unchanged. The focused complete development run passed all 13 tests in 2.4 minutes, including all 12 signature/mode combinations.

Recording review found that “What am I forgetting?” silently passed the portfolio harness despite an unsupported response. The existing global read-only recommendation intent now accepts that canonical phrase and exposes real candidate titles/reasons from the existing selector. Corpus, controller and real-UI coverage verify it on eight routes, with exact serialized state/history unchanged. Unrelated unsupported speech remains side-effect free. Portfolio beats now assert a supported result; the query beat also verifies no data/history write. No second recommendation algorithm or mutation path was added.

The first full development run then exposed a separate startup problem: the unchanged first-acquisition case timed out before the page loaded. Native Chrome NetLog measured request headers at 39 ms but response headers at 32,834 ms. An opt-in Node CPU profile attributed 57,036 ms of sampled self time to `@tailwindcss/vite` generation/scanning, versus 1,451 ms to GC. Tailwind's implicit root scan included accumulated HTML/report evidence; Vite watch ignores did not constrain that scanner. `@import "tailwindcss" source("./")` now scopes discovery to the real `src` directory containing all production classes and semantic tokens. No server timeout, performance limit or retry was changed. The fresh unchanged acquisition and all 12 development signature cases then passed; production native route, geometry/history and eight-route recommendation checks passed 3/3, and the strengthened portfolio passed 1/1.

The prior automated candidate and exact failed iterations are preserved under `artifacts/reward-resume-diagnostics/independent-gate-failure/`, the `dev-*` diagnostic directories and `dev-cold-netlog.json`. The CPU profile is `dev-startup-profile/CPU.20260905.140416.13.0.001.cpuprofile`. These targeted repairs still require the complete fresh release and independent re-assessment; they do not turn the earlier independent FAIL into PASS.
