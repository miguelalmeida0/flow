# Flow visual reward mission checkpoint

Saved: 2026-09-05, Europe/Berlin
Workspace: `~/Downloads/flow-voice-calendar`
Mission source: `~/.codex/attachments/dfdc18a0-5cb6-4525-a653-fa8f5f615d61/pasted-text.txt`

## Stop state

> **Superseded by resumed work on 2026-09-05.** The stop snapshot below is historical. The user subsequently requested completion. Current implementation/evidence status is in the resume update immediately below; do not restart the already resolved eleven-animation investigation.

## Resume update — active, not yet released

### Latest status — 2026-09-05 13:22 UTC

Update at 13:42 UTC: the narrow surface/text ownership repair passes two focused production tests and root reviewed all seven new route-entry images; the design-implementation gate is restored to passed, not release approval. Development six-signature coverage is being implemented. Root additionally cross-checked the portfolio's red “What am I forgetting?” response against the explicitly documented recommendation journey, confirmed a missing read-only intent alias plus missing portfolio semantic assertion, and assigned that exact defect to the principal before source freeze. No new service or parallel mutation path is authorized. Root's earlier review artifacts were copied to `artifacts/reward-resume-diagnostics/independent-gate-failure/root-review/`.

Further progress: all twelve new development signature cases passed focused validation, with real React development bundle type, StrictEffectsMode and observed probes. The recommendation repair passed 294 focused parser/controller tests and now has explicit browser/portfolio expectations. The complete development run still exposed cold first-request stalls in the unchanged acquisition test. Native NetLog showed immediate TCP/request delivery but delayed response; the preserved Vite CPU profile then attributed 57,036 ms to Tailwind's implicit whole-repository source scanner. The principal is restricting Tailwind source discovery to application source, not increasing test timeouts or changing unrelated Vite dependency options. Fresh full validation and independent QA remain outstanding.

Later frozen-run status: the first complete run caught an undefined test helper caused by a missing import after signature-fixture extraction; the run was preserved, exactly one source line was repaired and all 212 inputs were re-frozen. The next complete run passed lint, 805 tests, build and design migration, then failed two unchanged motion-evidence budgets: normal estimated missed-frame rate 9.8667% (37/338, limit 8%) and no native Outcome/tablet/20% frame within ±60 ms of 224 ms. Max normal gap 100 ms, longest task 76 ms, throttled rate 0.2208%, max throttled gap 166.7 ms, longest throttled task 92 ms, CLS/DOM/listeners/residue remained within bounds; 120/120 signature interruptions passed. While that same run was still active, root captured read-only Docker stats showing two unrelated `vigia-visual-qa` capture containers at 222.87% and 103.66% CPU, ClickHouse at 105.85%, Vigia Postgres at 77.03%, and Continuum at 23.05%, while Flow itself was at 0.42%. This is concrete concurrent-host-load context, not a waiver or automatic product pass. Do not stop those user processes. Preserve the complete artifacts and distinguish product attribution from host cadence before any edit or rerun; budgets remain unchanged.

The complete release subsequently passed (manifest `2026-09-05T13:06:48.762Z`): seven exit-zero commands, 793 tests / 30 files, 11,788 semantic cases, 71 production Chromium + one development acquisition, four motion cases, 120 signature cuts, 144 native frames, and zero browser errors. The unchanged cold legacy guard passed twenty transitions with zero long tasks. Root inspected all final 144 frames, exact-size reference comparison, and the complete portfolio video through decoded 2 fps sheets.

Independent `reward_final_qa` / `flow_qa` returned **FAIL** with two exact findings: (1) Home → Today's tall-card/shallow-header shared layout stretches plain descendant text and overlaps Now; native video and raw DOM trace confirm it. (2) The six signatures were not exercised under actual development StrictMode double-effect lifecycle behavior; production wrapping and the lone acquisition test are insufficient. Root returned the full failure report to existing `reward_principal_resume`, now actively repairing as sole production/test writer. Root owns this checkpoint, `docs/quality/design-qa.md`, release report and review artifacts. Preserve the passing candidate and failed visual evidence before runners overwrite artifacts. Run focused fixes, then fresh complete validation and independent QA again. No completion claim until independent PASS. Physical microphone remains NOT RUN; no Git commit available. The bullets below describe the preceding candidate and are historical.

- Root resumed the existing custom `flow_principal` as `reward_principal_resume`; it is the sole production/test writer. Root owns review attachments, `docs/quality/design-qa.md`, the thirty-section release report and this checkpoint.
- The eleven apparent surviving animations were traced to mixed simulated and native clocks. Motion evidence now preserves native rAF/performance/timers/WAAPI with Date-only fixture offset. Real OS/in-app Reduced ownership and invisible importance markers were also repaired.
- Further validated repairs include mobile milestone placement, measured Home mascot exclusion, sparse semantic mascot presence, user-facing flight copy, preservation of the original Capture-to-Outcome transaction reward, cached clock/date formatting, and geometry-dependent calendar measurement. Scheduling, persistence and global history remain authoritative and were not replaced.
- The latest full release attempt passed lint, **793 tests in 30 files**, strict TypeScript/build, design migration, and all four motion cases: normal/throttled performance, **120/120 signature stress cases**, **144/144 frame assertions**, and portfolio recording.
- The outer release then stopped on the existing cold-morph guard: **87 ms observed main-thread task > unchanged 50 ms budget**. Functional production/development suites did not execute in that attempt. This is **FAIL**, not complete. Diagnostic evidence is preserved under `artifacts/reward-resume-diagnostics/`.
- Root visually inspected all **144 fresh corrected native-compositor frames** in twenty-four regenerated sheets after the targeted browser matrix passed. The settled states, protected anchors, durable completion results and mobile heading repairs hold. Both findings are resolved: paper resizes independently of unscaled co-moving text, and distinct native compositor frames now cover all intermediate samples.
- Exact earlier failures remain preserved in `artifacts/reward-resume-diagnostics/visual-review-before-native-screencast/`. Current `docs/quality/design-qa.md` verdict is **PASSED for design implementation**, not full release. All 96 intermediate samples are within 9.70 ms of the nominal deadline, with nearest native-audit skew at most 8.34 ms. Time travel uses its unchanged 480 ms scene transition. No synthetic poses, paused clocks or duration inflation.
- The principal is authorized to attribute the legacy long task and run existing functional tests independently for diagnosis, without changing thresholds/retries or calling a partial run a release pass. Do not begin another full release before bounded fixes/validation and root visual approval.
- Fresh independent `flow_qa` remains **NOT RUN**. Spawn it only after the principal completes a fully green fresh release; route exact failures back until PASS.
- The isolated native legacy-morph attribution case passed (20 transitions, no transition long task); it does not waive the earlier 87 ms failure. The unchanged 70-test production functional suite is running diagnostically before the fresh full release.
- Remaining root steps: after the complete release, inspect its regenerated 144 frames, compare the locked reference at 1672 × 941, inspect the new portfolio recording, refresh source hashes, verify the live preview, finalize report and independent verdict.
- Physical acoustic microphone acceptance remains **NOT RUN**, separate from synthetic tests. No Git metadata is available; no commit is fabricated.

## Historical stop snapshot

- Work was stopped immediately at the user's request.
- The active `reward_principal` agent was interrupted. No implementation or test agent remains running.
- No Flow Playwright/Docker QA container is running. The latest authoritative motion process had already exited with a failure before the stop request.
- The existing Vite preview remains intentionally available at `http://127.0.0.1:4174/` because it is open in the user's browser. Its listener was PID 9205 at checkpoint time.
- No commit was created: this directory has no Git repository metadata.
- Do not treat the release as complete. Independent `flow_qa` has not returned PASS after the latest fixes.

## Required agent workflow state

Completed in the mandated order:

1. `flow_mapper` report complete.
2. `flow_language_auditor` report complete.
3. `flow_principal` implemented and validated the first release candidate.
4. Independent `flow_qa` inspected that candidate and returned FAIL with four exact defects.
5. The exact QA failure report was routed back to `flow_principal`.
6. `flow_principal` implemented the repairs below, but the repeat independent QA cycle has not happened because the fresh motion gate is still red.

Resume with the existing `flow_principal` first. Do not spawn a new `flow_qa` until the principal produces a fully green fresh `qa:motion` and `qa:release` evidence bundle.

## Independent QA failures and repair status

### 1. Production voice-understood reward — repaired, targeted green

- Production now emits a bounded `voice-understood` event from `FlowEnvironmentProvider` through the real final-recognition path.
- Production Chromium reward coverage verifies the order `voice-understood` → `transaction-committed`.
- Last targeted reward suite result reported by the principal: **9/9 PASS**.

### 2. Six-signature 20× stress matrix — implemented, targeted green, authoritative run red

- `e2e/visual-reward-performance.spec.ts` now exercises all six signatures:
  - Tide recovery
  - Focus completion
  - Outcome completion
  - Commitment kept
  - Whole-world time travel
  - Capture → Outcome
- Each signature runs 20 times: 5 full/normal, 5 reduced/normal, 5 full/4× CPU, and 5 reduced/4× CPU.
- Cross-cuts include immediate typed Undo/Redo, navigation, viewport resize, visibility interruption, sound disabled, mascot minimal, and settled-residue checks.
- Last standalone targeted matrix reported by the principal: **120/120 PASS in 5.9 minutes**.
- The fresh authoritative `qa:motion` run later failed early in this matrix; see the exact blocker below.

### 3. Browser Back reward — repaired, targeted green

- Browser Back remains functional but maps to Level 0 / no recipe instead of a new Tide reward.
- Unit and production Chromium coverage were updated.

### 4. Mascot collision — repaired, frame matrix green

- The supplementary mascot is now omitted from task worlds including Today, and only occupies Home's reserved desktop safe zone.
- It is omitted below 1280 px.
- Frame inspection now includes collision checks, not viewport containment alone.
- The fresh 144-frame matrix passed after this change.

### Additional defects found and repaired during validation

- Removed stale `lastTranscript` → command-draft backflow that could overwrite a rapidly typed `Redo` with the completed `Undo` transcript.
- Rapid typed Undo/Redo now polls persisted history state rather than relying on transient dock paint.
- Stress uses typed global Undo/Redo on every route; it no longer tries to pointer-click an intentionally keyboard-only compatibility control.
- Focus stress uses a persisted elapsed-time fixture instead of overriding `Date`.
- Time-travel stress pins the browser clock to the release date so midnight cannot invalidate deterministic assertions.
- Today no longer renders a mascot over calendar facts at any viewport.

## Exact current blocker

Latest fresh authoritative command:

```text
npm run qa:motion
```

Latest machine result:

```text
artifacts/visual-reward-qa/results.json
generatedAt: 2026-09-04T22:41:14.658Z
verdict: FAIL
Playwright exit code: 1
3 passed / 1 failed (four tests actually executed; the results summary field reports three expected)
```

Passing parts of that same fresh run:

- base 20-cycle performance audit: PASS
- 144/144 canonical frames: PASS and inspected
- portfolio recording: present
- two trace archives: present
- console/page/request errors: all empty
- visual frame and recording tests: PASS

Failing assertion:

```text
every signature survives 20 interrupted full/reduced and normal/throttled production runs
expected running animations after settle: 0
received: 11
```

Failure point from `signature-stress-progress.json`:

```text
mode: reduced-normal
signature: tide-recovery
run: 1
stage: settling-cross-cuts
completed before failure: all 30 full-normal runs
cross-cuts completed before failure: 30
```

Exact evidence:

- `artifacts/visual-reward-qa/qa-motion.log`
- `artifacts/visual-reward-qa/results.json`
- `artifacts/visual-reward-qa/signature-stress-progress.json`
- `artifacts/visual-reward-qa/playwright-results/visual-reward-performance--7c980-l-throttled-production-runs-visual-reward-performance/error-context.md`
- failure screenshot beside that error context

The next engineering task is to identify the 11 `document.getAnimations()` entries still running 900 ms after the reduced-normal Tide recovery cross-cut settles. Do not weaken the zero-animation assertion or merely wait longer without proving ownership and cleanup. Instrument animation targets/names/play states, repair the owner, then rerun the complete 120-run matrix.

## Evidence freshness warning

`artifacts/release-qa/results.json` is the last pre-independent-QA full release PASS:

```text
timestamp: 2026-09-04T21:05:13.814Z
27 files / 776 tests
68 Chromium tests
releaseVerdict: PASS
```

It predates the independent QA repairs above and is therefore **stale for the current code**. It may be used as historical evidence only. A complete new `npm run qa:release` is required after the current motion failure is fixed.

`docs/quality/VERIFICATION.md` intentionally still says independent `flow_qa`: **PENDING**. Do not change it to PASS until a new independent QA agent passes the fresh post-fix artifacts.

## Resume sequence

1. Resume `flow_principal` with this checkpoint and the current error context.
2. Diagnose the 11 surviving animations by logging their target element, animation name/keyframes, duration, and owner at the failing reduced-normal Tide settle.
3. Fix the production/test lifecycle cause without relaxing zero-residue, timeouts, performance budgets, retries, or signature coverage.
4. Rerun the targeted 120-run signature matrix until PASS.
5. Rerun complete `npm run qa:motion` until PASS, preserving 144/144 frames, collision checks, two traces, portfolio video, performance, and clean browser arrays.
6. Rerun complete `npm run qa:release` from zero.
7. Rebuild the contact sheets if the final frames changed and re-review them with the locked reference.
8. Update `docs/quality/VERIFICATION.md`, `docs/quality/design-qa.md`, and motion docs with only the fresh final counts/metrics; keep independent QA PENDING.
9. Spawn a fresh independent `flow_qa` and give it both the original failure report and the repaired evidence.
10. If it fails, route the exact report back to `flow_principal`; repeat until `flow_qa` returns PASS.
11. Only then update `docs/quality/VERIFICATION.md` to independent PASS and produce the 30-section final report.

## Current honest product status

- Implementation: materially complete, with all four first-pass independent QA defects repaired in code.
- Targeted reward suite: PASS 9/9.
- Targeted full signature stress: PASS 120/120 before the fresh gate.
- Fresh canonical visual matrix: PASS 144/144.
- Fresh motion release gate: FAIL because 11 animations remained running in one reduced-normal settle.
- Fresh full release after QA repairs: NOT RUN.
- Repeat independent `flow_qa`: NOT RUN.
- Physical Chrome microphone acceptance: NOT RUN.

No production code was modified while saving this checkpoint.
